/**
 * Problem: [Problem Name]
 * Difficulty: [Easy/Medium/Hard]
 * LeetCode #: [Problem Number]
 *
 * [Problem Description]
 * [Explain the problem in detail, including input/output format]
 *
 * Example 1:
 * Input: [example input]
 * Output: [example output]
 * Explanation: [explanation]
 *
 * Example 2:
 * Input: [example input]
 * Output: [example output]
 *
 * Constraints:
 * - [Constraint 1]
 * - [Constraint 2]
 * - [Constraint 3]
 */

/**
 * Approach: [Name of approach]
 *
 * Algorithm:
 * 1. [Step 1]
 * 2. [Step 2]
 * 3. [Step 3]
 * ...
 *
 * Time Complexity: [Big O notation] - [Explanation]
 * Space Complexity: [Big O notation] - [Explanation]
 */
function solve([parameters]) {
	// Your solution code here

	return result;
}

// Export for testing
export default solve;

/*
 * TESTING INSTRUCTIONS
 * ===================
 *
 * To test this solution:
 *
 * 1. Create a spec file in the __spec__ folder:
 *    [category]/__spec__/[problem-name].spec.js
 *
 * 2. Add your test cases using Jest:
 *
 *    import solve from "../[problem-name].js";
 *
 *    describe("[Problem Name]", () => {
 *      test("Example 1", () => {
 *        expect(solve([input])).toBe([expected]);
 *      });
 *
 *      test("Edge case", () => {
 *        expect(solve([input])).toBe([expected]);
 *      });
 *    });
 *
 * 3. Run the tests:
 *    npm test -- [category]/__spec__/[problem-name].spec.js
 *
 * OR run tests in watch mode:
 *    npm run test:watch
 *
 * Good luck! 🚀
 */
